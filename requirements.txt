numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0                 # optional — attacker_model falls back to scikit-learn's GradientBoostingClassifier if not installed
sentence-transformers>=2.2   # optional — module falls back to a hashing embedder if not installed
requests>=2.31
beautifulsoup4>=4.12
pytest>=7.0                  # for running the test_*.py files
